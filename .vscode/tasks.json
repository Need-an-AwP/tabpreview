// See https://go.microsoft.com/fwlink/?LinkId=733558
// for the documentation about the tasks.json format
{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "build:webview",
			"type": "npm",
			"script": "build:webview", // run build of the sub vite project
			"presentation": {
				"reveal": "always",
				"panel": "dedicated"
			}
		},
		{
			"type": "npm",
			"script": "compile", // "watch" is replaced by "compile"
			"problemMatcher": "$tsc-watch",
			"isBackground": true,
			"presentation": {
				"reveal": "never"
			},
			"group": {
				"kind": "build",
				"isDefault": true
			},
			"dependsOn": [
				"build:webview" // ensure webview is built before starting watch
			]
		}
	]
}
